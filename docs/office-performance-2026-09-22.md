# 홈페이지 성능·터치 개선 — 2026-09-22

이번 범위는 초기 파일 전송, 모바일 처리 부담, 작은 물체의 터치 안정성이다. 방의 구성과 시각 디자인은 유지했다. 현재 로컬 미리보기에 반영했으며 운영 배포는 하지 않았다.

검증 빌드: `7bc9255-muccukzf`.

## 변경과 확인 결과

- **모바일 최초 전송량 약 11% 감소:** 3회 측정 중앙값 38,111,336 → 33,912,741바이트. 기존 모바일 표시 해상도에 맞춘 기타 파일은 6,576,992 → 2,378,340바이트로 줄었다. 데스크톱은 원본을 사용한다.
- **속도를 제한한 연결에서 대기 시간 감소:** 지연 80ms·다운로드 12Mbps 조건으로 원본과 개선 파일을 각각 3회 불러왔다. 방 준비 시간 중앙값은 25.83 → 23.09초로 약 2.74초(10.6%) 줄었다. 전체 방 준비 조건은 같으며, 해외 실측 결과는 아니다.
- **화질·형태 유지:** 기타의 압축된 형상 데이터는 바이트 단위로 동일하다. 동일한 날짜·조명·카메라에서 원본과 새 파일의 화면을 비교해 304,500픽셀 모두 동일한 것을 확인했다.
- **터치 오작동 방지:** 물체를 누른 뒤 드래그했다가 원래 위치로 돌아오거나 두 번째 손가락을 대면 클릭을 취소한다. 기존 한 번 누르기와 두 번 눌러 확대하기는 유지한다.
- **CD 버튼 누르기 개선:** 모바일에서 작은 물리 버튼 사이의 빈 공간까지 보이지 않는 터치 영역을 넓혔다. 인접 버튼 영역은 겹치지 않는다. 기존 영역 바깥에서 PLAY를 눌러 실제 음원이 재생되고 PAUSE로 멈추는 것을 확인했다.
- **불필요한 입력 처리 감소:** 대기 상태의 전역 pointermove 처리기가 50개에서 3개로 줄었다. 필요한 추적은 물체를 누른 동안에만 켠다.
- **읽기 중 배경 처리 감소:** 읽기 패널 스크롤 때문에 배경 방이 60fps 모드로 올라가지 않도록 했다. 실제 측정 약 30.2fps이며, 방 조작 시의 렌더링 주기는 유지한다. 실물 휴대폰 발열·배터리 개선량을 측정한 것은 아니다.

## 검증 범위

- 최종 전체 단위 검사 365개 통과. 타입 검사 오류·경고 0, 공개 빌드 파일 2,389개 검사 통과.
- 로딩·재시도, 읽기, TV·콘텐츠, 응답 정책, 음악·CD 교체, 최초 물체 표시·외벽의 기존 브라우저 검사 6종 통과. 새 터치 검사도 통과하고 이후 배포 검사 목록에 포함했다.
- 데스크톱·태블릿·모바일 최초 준비 시점에 주요 물체 13개, CD 케이스 10개, 책등 8개가 준비됨을 확인했다. 기타·앰프의 지연 등장을 다시 도입하지 않았다.
- 동일 조건 기타 비교와 모바일 조작을 포함한 핵심 캡처 14개를 독립적인 두 검토에서 확인했으며, 기존 디자인·동작을 훼손하는 문제는 발견되지 않았다.

## 아직 남은 성능 과제

빠른 로컬 연결에서는 방 준비 시간 중앙값이 모바일 5.60 → 5.74초였다. 따라서 이번 결과를 모든 환경에서 로딩이 빨라졌다고 표현하지 않는다.

로컬 정적 서버의 Lighthouse 모바일 3회 결과는 성능 25·25·30, 접근성 100, 권장사항 96, SEO 100이었다. 초기 3D 스크립트 실행 비용과 여전히 큰 전체 자산량이 남아 있다. 로컬 서버는 운영 CDN의 압축 전달을 재현하지 않으므로 이 점수를 운영 사이트 측정값으로 사용하지 않는다. 자동 접근성 점수 역시 모든 사용자의 접근성을 보증하지 않는다.

같은 Lighthouse 실행의 데스크톱 단계는 브라우저 대상 연결이 닫혀 완료되지 않았다. 이 실행의 데스크톱 성능 점수는 없다. 별도 데스크톱 기능·시각 검사는 통과했다.

다음 성능 작업은 초기 3D 준비 중 긴 작업을 찾아 나누는 데 집중해야 한다. 이미 보이는 물체를 숨기거나 뒤늦게 나타나게 하는 방식으로 해결하지 않는다.

## 증거

`.omo/evidence/office-performance-2026-09-22/`에 원본/개선 측정, 캡처, 실제 오디오 조작 결과와 로그를 보존했다. 주요 위치는 `final-performance/`, `final-guitar/`, `final-touch/`, `final-browser/`, `network-loading.json`, `lighthouse-summary.json`, `performance-visual-review.md`다.
