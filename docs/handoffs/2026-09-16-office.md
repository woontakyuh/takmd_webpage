# TakMD office handoff — 2026-09-16

## Start here

Repository: `/Users/TakMD/workspace/takmd-com`.
Working branch: `feat/desk-scene-3d`; remote: `origin` (`woontakyuh/takmd_webpage`).
Production: https://takmd.com, Cloudflare Pages project `takmdwebpage`, production branch `main`.
Cloudflare production branch is a deployment parameter, not this Git working branch.
The release receipt is `.omo/evidence/controls-release-2026-09-16/release.json` after deployment. Check that receipt and current remote/provider state before reporting a release.

## This checkpoint

- Controls help: action first, one row each. Desktop Back says X / Esc; mobile Back says X only. Same existing materials and room controls.
- Research guided tab now approaches a CLOSED folio. Click the physical folio to open it and read; closing returns to the closed approach.
- Guided views have an italic title matching “The office.” and an unboxed X. X, Escape and Overview return to the room. Names: Research, Talks & Recognition, UBE & Teaching, Whisky & Music, Jiu-jitsu & Surfing.
- Window-side objects centered as a group without changing their order: endoscope tray → dummy → pig → spine.
- Surfboard fin: user approved both preview images on September16. One printed face; the reverse naturally mirrors and softens that same print through fiberglass. Planar UVs preserve source proportions; do NOT independently make both faces read forwards. Source markings are not retyped. `finArtwork.ts` keeps photographed background off the fin edge and preserves ink. `bingSurfboard.test.mjs` verifies identical paired-face UVs and uniform image scale.
- CD exchange: remove the outgoing-cover offset clamp. Catalog order/identity stays fixed while the entire carousel pans to the outgoing case, then back to the selected incoming case. Two timed phases give each movement time to settle. A mounted disc moved between slots travels directly between the slots after the displaced disc returns.
- CD placement: choose the actual player slot; no text grid beneath the covers. Transparent hit targets project onto physical discs. Existing HoverAccent lights the actual geometry, with unboxed CD number/album text above the hovered/focused disc. Touch taps work. Selecting a track before placement explicitly starts that track; simple placement does not autoplay. Cancel/Escape and closing clear placement state.

## User design rules

- Preserve the room and original supplied imagery. No large boxes covering the objects, no generated substitute logos/type/photos.
- Use the existing soft material glow (`HoverAccent.tsx`), not flashy motion or outlines.
- CoverFlow is transparent and room-native. Never swap individual cover art to fake a return animation.
- CDP faceplate is English PLAY / PAUSE; global player is an icon. Volume is short, thin and unboxed.
- Mobile readers expand smoothly with scrolling; desktop readers can stay alongside the scene.
- The room is the homepage experience; no unsolicited below-room content.
- Public content must stay de-identified. Workshop source data restrictions still apply: do not modify `content/workshops/`, `public/images/workshops/`, or `src/data/workshop-*.ts` without the user's direction.

## Evidence / commands

- `.omo/evidence/cd-final-2026-09-16/qa.mjs`: real Chrome interaction at390×844 and1440×900; physical slot selection, every cover retaining catalog order, outgoing/incoming travel, explicit audio playback. PNGs: slot/return/insert for each viewport; JSON motion samples.
- `.omo/evidence/cd-final-2026-09-16/touch-qa.mjs`: emulated touch + reduced-motion, cancellation, moving to an occupied slot, returning and placing into an empty slot without autoplay.
- `.omo/evidence/fin-repair-2026-09-16/navigation-qa.mjs`: closed Research approach, physical click, closing, guided X and Escape at desktop/mobile widths.
- `.omo/evidence/fin-repair-2026-09-16/{fin-negative,fin-positive,room}.png`: approved fin sides / centered teaching display.
- `bun run check`
- `bun test src/components/studio/scene/Beosound9000State.test.mjs src/components/studio/scene/BeosoundCollection.test.mjs src/components/studio/scene/BeosoundLibrary.test.mjs src/components/studio/scene/BeosoundCoverFlow.test.mjs scripts/bingSurfboard.test.mjs`
- `bun run build` includes current-poster capture and public asset hygiene check.

Browser validation uses desktop Chromium with mobile viewport/touch emulation. Do not call it physical iPhone/Safari testing. Long-running end-of-track playback on actual Safari remains a device-specific check, not a proven result of these scenarios.

## Release safety and local leftovers

There are MANY unrelated untracked assets, old screenshots, experiments, `Beolab8.tsx`, `ArtworkCalendar.tsx`, and old generated posters. Do not `git add .` and do not build/deploy root public blindly.
Build an isolated Git-tracked export, copy only intended changes/new files, and keep its dependencies local (symlinked node_modules caused Vite font403s). Poster fingerprint covers tracked source/assets; generated poster files and manifest are excluded from the hash. After build, copy the new manifest and its eight referenced files back and commit those exact files.
Deploy the verified export's `dist` with Wrangler4.129.0 and the actual Git HEAD. Confirm remote SHA, Cloudflare Production deployment, public HTML/JS/CSS/posters and changed GLB bytes separately. Do not infer release from a local preview.

No additional feature development is authorized as part of this handoff. User wants to pause homepage work at this checkpoint and may resume with Claude Code. New requests should start from this checkpoint; do not replay old completed requests from the long session.
