# TakMD office handoff — 2026-09-16 (Claude Code takes over)

## Start here

Development moved from ChatGPT Astra to Claude Code on 2026-09-16, in this checkout on `feat/desk-scene-3d`.
Read [the Astra final handoff](2026-09-16-office.md) for the room's design rules, release procedure and the state before this session. Everything below is newer.

## This checkpoint

- **Safari blank readers (root cause of the "TV opens the old page" report).** `TvScreenReader` and `MonitorScreenReader` used drei's `Html transform` mode. Its inner layer is scaled to metres (about 0.006) and magnified back through CSS perspective; WebKit rasterises the layer at the tiny scale, so Safari showed a blank TV and monitor while the reader's controls stayed tappable. Taps on the blank TV landed on invisible controls, most often the `Teaching & training` link below the screen, whose Education panel is the legacy `/education` content. Both readers now use a screen-aligned overlay (`center` + viewport-aware `distanceFactor`); they only mount once the camera faces the screen head-on, so geometry matches the bezel to 0.1 px in Chromium and paints in WebKit. Chromium-only QA had never exercised this path.
- **Header = office title.** The upper-left brand block (mark, name, caption) is gone. The italic office title with the `TAKMD / A PLACE TO THINK` kicker is the page `h1` upper-left; guided views rename it and keep the close control beside it. Guided-view buttons stay lower-right; Living CV / Contact stay upper-right.

## Evidence / commands

- `.omo/evidence/tv-reader-webkit-2026-09-16/qa.mjs <url> [engines] [viewports]`: opens the TV by a physical tap and the monitor CV, then measures the light-pixel share of each reader in WebKit and Chromium at 390×844 and 1440×900. Red on the previous production (0 in WebKit), green after the fix on dev, built preview and live. `release.json` there is the receipt.
- `.omo/evidence/header-title-2026-09-16/`: check/build logs and receipt for the header change.
- WebKit runs with Playwright's bundled build: `WK=~/Library/Caches/ms-playwright/webkit-2336/pw_run.sh`. It is the Safari engine, not a physical iPhone.
- `scripts/test-monitor-cv.mjs`, `test-tv-slide-fit.mjs`, `test-monitor-registration.mjs` fail identically on the previous production (stale selectors such as `Browse lectures`); they are not part of the current release gate.
- Release procedure unchanged: build in the checkout for posters, commit the manifest and its eight files, then `git archive HEAD` into a scratch export, `bun install`, `bun run build`, `wrangler pages deploy dist --project-name takmdwebpage --branch main`. Never `git add .` here; 170+ untracked leftovers remain in the checkout.

## Open items

- `claude/work` (11 commits: workshop sessions, 128 photos) merges cleanly into `feat/desk-scene-3d` but is not merged; the office's `/workshops` pages still show the older data. The separate team site lives in `workspace/spinoscopy-workshop` with a copy of the same data files; the user wants that site to become the source of truth and takmd.com to consume it.
- Physical iOS Safari confirmation of the reader fix is still outstanding.
