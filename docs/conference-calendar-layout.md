# Conference calendar layout — 2026-09-11

Preserve the existing warm paper, serif month title, clay event ink, weekday rules and source snapshot. One conference is one continuous bar across its date columns, split only at visible week/page boundaries; pointed open ends indicate continuation. Sort by start date, then longer ranges, then stable event ID, and keep a conference in one lane throughout the visible page. Visible neighboring-month cells retain the actual range.

The paper height and six equal week rows depend only on viewport dimensions. Previous/next arrows remain anchored to the same position in that fixed paper, including empty and busy months. Only the event ledger scrolls. Desktop offers two lanes, compact or short viewports one, with a small +count access point for additional events rather than taller weeks. Clicking or keyboard-activating any bar focuses its full details in the ledger. Date cells keep full date/event labels for assistive reading.

Reuse existing 9–11px sans labels and the calendar ink/accent/action colors. Range strips use clay at 16% over the original paper and 25% on hover, with 4px continuation cuts and 2px true endpoint rules. Keep the native close, month navigation and focus rings. The desktop sheet uses its existing 760px width cap and a fixed height up to 820px inside the viewport; compact paper retains the lower-screen placement while reserving enough height for all six weeks. No event dates or participation data change.

During object inspection, the room stage and document body are clipped, and the body is fixed to the viewport. The body clip rule must override the inline `overflow: hidden` used by existing inspection surfaces: hidden overflow still permits native focus and `scrollIntoView` to scroll a fixed body. The observed failure left window/stage offsets at zero while body.scrollTop reached 372px and shifted the entire calendar above the screen. Keep the ledger's own overflow auto; closing inspection releases the document for the ordinary page below the room. Browser regression coverage includes actual repeated month clicks at identical coordinates, all three scroll offsets, ledger scrolling and release.
